# BlueShelves App

This is an Expo project bootstrapped with create-expo-app.

## Getting Started

First, install dependencies and run the development server:

    npx expo start --go
    # or
    yarn expo start --go
    # or
    pnpm expo start --go

Open the Expo Go app on your device and scan the QR code from the terminal, or run it in an emulator.
